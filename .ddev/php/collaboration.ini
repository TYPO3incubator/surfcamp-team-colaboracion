; Increase PHP-FPM workers for SSE connections
; Each SSE connection holds a PHP worker in a long-running loop.
; Default 8 is too low for multiple concurrent backend users.
pm.max_children = 30
pm.start_servers = 5
pm.min_spare_servers = 3
pm.max_spare_servers = 10
